// var mongoose = require('mongoose');
// var Schema = mongoose.Schema;

// var userSchema = new Schema({
// 	username: String,   // Must be 4(?) chars long, can contain any characters
// 	password: String,   // Use hashing function to store encrypted password
// 	firstName: String,  // Only alpha chars, make sure first letter is capitalized 
// 	lastName: String,   // Only alpha chars, make sure first letter is capitalized 
// 	email: String,      // Must be in format 'youremailhere@domain.com'
// 	userID: Number      // Number to be a reference for main DB 
// }); 